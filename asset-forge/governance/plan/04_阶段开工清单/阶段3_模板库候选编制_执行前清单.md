# 阶段3执行前清单：模板库候选编制

- Stage ID: `S3`
- Required task book: `..\02_长程任务书\阶段3_模板库候选编制_目标4_长程任务书.md`
- 完整性说明: 本文件曾被 PowerShell 5.1 ANSI 读写破坏，2026-08-30 按逐字原文重建（tick 附证据）；见 stage-4 cleanup 报告缺陷 #4。

## 硬门槛

- [x] 已读取 workspace/计划 README、manifest、总控、G4 和 Stage 3 任务书。（2026-08-30 Round 3/4）
- [x] Stage 0..2 完成门通过；pageTrainType/validator 映射、来源统计和知识边界已冻结。（gate 记录 pass-with-boundary/pass-with-boundary/pass；PAGE_SPECS/accepted 集合源码级核对入 `05_模板库候选/template-coverage-matrix.csv`）
- [x] 已创建 `05_模板库候选`、`research-rejected` 和 `evidence/stage-3`。（2026-08-30）
- [x] 已确认候选 TOML 只放根目录顶层，证据卡不混入正式模板扩展名。（`.evidence.md` 配对 + 碰撞守卫内嵌 stage3 runner）
- [x] 已确认每份模板显式 `model_train_type`，不会因 scope 无法解析而 `[skip]`。（5/5 REQUIRED_KEYS 硬检查；拒绝草案亦含显式类型）
- [x] 已定位真实 project validator 并使用 Python `-B`，避免污染源仓。（`.venv-dev` python；utils `__pycache__` 时间戳证明零 bytecode 写入）
- [x] 已定义 parse/secret/path/field/validator/normalized diff 检查。（`tools/stage3_validate_templates.py` 全项 + 阴性对照 + deny 断言 + zero-short）
- [x] 已确认 Civitai 样本不足时只用项目/官方基线并明确边界。（值仅出厂 preset/schema 默认/家族事实三类 L1；unknown 显式空缺，Flux 为 C-014 正面案例）
- [x] 已确认不执行训练、不保证效果、不迁移。（无训练动作；效果声明仅存在于边界/空缺表述；出口停在候选区）

## 本阶段禁止事项

1. 把 `skip`、未运行或仅 TOML parse 写成 validator pass。
2. 为了"模板多"复制近似配置，或保留机器路径/token/真实数据集名。
3. 将 reject 草案留在候选根目录。

## 不满足时处理

validator 不可用或页面映射不明视为 P1，停止该页面模板编制并生成 failure report。
