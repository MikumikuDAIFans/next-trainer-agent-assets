# 阶段2执行前清单：知识库候选编制

- Stage ID: `S2`
- Required task book: `..\02_长程任务书\阶段2_知识库候选编制_目标3_长程任务书.md`

## 硬门槛

- [x] 已读取 workspace/计划 README、manifest、总控、G3 和 Stage 2 任务书。（2026-08-30 goal 轮次完成；含全部预检治理文件与 MVP probe 报告）
- [x] Stage 0/1 完成门通过；支持矩阵、来源登记、缺失率和偏差报告已冻结。（均 `pass-with-boundary`；Stage 1 样本为 MVP exploratory，仅按 L2 观察使用；矩阵经 `9cd2399..a1a5797` 漂移复核仍有效，见 CR-003 与 `evidence/plan/drift-review-20260830.md`）
- [x] 已创建 `04_知识库候选` 与 `evidence/stage-2`。
- [x] 已先建立覆盖矩阵和文档 manifest，再开始批量写作。（`tools/stage2_build_coverage_matrix.py` 生成 `knowledge-coverage-matrix.csv` + `knowledge-manifest.jsonl`，可重放）
- [x] 已加载正式知识格式、评测追加和播种只补缺失规则。（agent-assets README 合同 + 14 篇现役文档三件套头实测 + eval README「新增必须追加评测条目」+ 播种只补缺失/用户文件主权）
- [x] 已确认每篇三件套头、来源卡、边界、别名和至少一条 eval。
- [x] 已确认 L1/L2/L3 分层，unknown 不变成建议。（Stage 1 结构化参数 0 条，本阶段禁止把 Civitai 观察写成精确参数）
- [x] 已确认不写未支持模型操作指南、不迁移正式资产。（Lumina 2/SD3/ControlNet/TI 仅边界文档；gray 基线为 14 知识/4 模板，CR-003）
- [x] 已定义 Markdown/link/source/sensitive lint 和失败处理。（`tools/stage2_lint.py`：头三件套、别名、来源卡、内部链接、机器路径、manifest 一致性；任一 fail 阻止该文档标记 ready）

## 本阶段禁止事项

1. 无来源写精确参数、显存门槛、兼容声明或效果结论。
2. 用文章数量替代支持×主题×方向覆盖证明。
3. 复制外部长文本或保留个人/机器路径。

## 不满足时处理

缺覆盖矩阵、稳定来源或格式规则时不得开始写作；生成 failure report 并退回 Stage 1 或 Phase 1。当前复核结果：`ready`。
