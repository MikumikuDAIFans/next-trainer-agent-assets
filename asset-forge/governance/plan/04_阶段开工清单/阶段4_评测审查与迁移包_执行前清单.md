# 阶段4执行前清单：评测审查与迁移包

- Stage ID: `S4`
- Required task book: `..\02_长程任务书\阶段4_评测审查与迁移包_目标5_长程任务书.md`
- 完整性说明: 本文件曾被 PowerShell 5.1 ANSI 读写破坏，2026-08-30 按逐字原文重建（tick 附证据）；见 stage-4 cleanup 报告缺陷 #4。

## 硬门槛

- [x] 已读取 workspace/计划 README、manifest、总控、G5 和 Stage 4 任务书。（2026-08-30 Phase 1 开工前）
- [x] Stage 0..3 完成门通过且候选批次已冻结。（pass-with-boundary/pass-with-boundary/pass/pass-with-boundary；40+5+5+1 冻结后只增证据不改内容）
- [x] 已记录正式 project/agent-assets Git status，与任务开始基线对照。（`evidence/stage-4/git-baseline-20260830.txt` vs `git-final-20260830.txt` = IDENTICAL）
- [x] 已创建 eval、final report、migration package 和 `evidence/stage-4` 目录。（`eval-candidates/`、`07_迁移包/`、`evidence/stage-4/` 就位）
- [x] 每个候选文件已有 eval 映射或阻断说明。（40/40 三方一致；5/5 模板 ok 工件；拒绝草案↔F-S3-001）
- [x] 已确认 migration manifest 只描述，不执行复制、sync、commit、push、build 或 release。（manifest `"execution": "NONE"` + git 双快照）
- [x] 已定义 hash、目标白名单、compat/eval 计数和播种文件名检查。（sha256×51、4 前缀白名单、计数草案 54/9/55/18、seed-if-missing+主权跳过）
- [x] Zero-Short 使用 AgentAssets 内临时目录，并有路径解析和清理规则。（`zero-short-tmp` finally 清理；exit 0、hash parity 50/50）
- [x] 已确认最终结论只允许 `awaiting-user-approval` 或 `not-ready`。（终态 = `awaiting-user-approval`，判定见 stage-completion-gate #12）
- [x] 失败/未运行/不适用检查会分别报告。（phase-1-report §三态清单：失败 0 / 未运行 live-agent 回放 / 不适用 GPU 实训）

## 本阶段禁止事项

1. 向正式知识、模板、eval、compat 或 vendored 快照写入。
2. 用"准备完成"推定用户已经批准迁移。
3. 删除失败证据、降级硬验证或隐藏正式仓状态变化。

## 不满足时处理

任一目标冲突、Git 状态异常、硬验证缺失或迁移覆盖风险都阻断 readiness，并生成 failure report。
