# 阶段0执行前清单：训练器能力盘点

- Stage ID: `S0`
- Required task book: `..\02_长程任务书\阶段0_训练器能力盘点_目标1_长程任务书.md`

## 硬门槛

- [x] 已读取 workspace/计划 README、manifest、总控和 G1 目标计划。
- [x] 已读取 Stage 0 任务书、测试治理、风险授权治理和 change control。
- [x] GATE-00..07、09、10、13 状态与 manifest 一致。
- [x] 已记录 project 与正式 agent-assets 的 Git branch/status，只作为基线。
- [x] 已确认 source project 和正式 agent-assets 全程只读。
- [x] 已创建 `01_训练器能力盘点` 和 `evidence/stage-0` 输出范围。
- [x] 已定义扫描排除项（`.git`、依赖、build/runtime/cache、大文件）。
- [x] 已确认不启动 GUI、训练任务、依赖安装或写配置。
- [x] 已确认失败时生成 failure report，不把 unknown 推成 supported。

## 本阶段禁止事项

1. 修改 project/agent-assets；运行训练、同步、构建或提交。
2. 只凭 README、文件名或底层依赖存在就裁定 first-class 支持。

## 不满足时处理

任一硬门槛不满足，停止 Stage 0，并按 `failure-report-template.md` 写 preflight failure report。
