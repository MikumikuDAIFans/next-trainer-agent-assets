# 阶段7：广泛外部渠道与知识模板扩充长程任务书（目标8）

## 计划元数据

- Plan ID: `NT-ASSET-KB-TPL-20260829-S7`
- Version: `v1`
- Canonical progress file: `..\00_总控目标索引.md`
- Change record: `..\00_预检证据\change-record-009-broad-channel-expansion.md`
- Current active phase: `complete (pass-with-boundary)`
- Execution readiness: `closed; migration not authorized`

## Goal

在既有 Stage 6 基础上补齐可复用的外部信息收集流程与渠道，优先扩展模型卡/API、官方 examples/config、数据集与标注、评测和运行时证据；将可靠短事实转为候选知识，将可由当前宿主证明的组合转为候选模板，其余保留为比较或研究边界。

## Scope and constraints

- 只写 `E:\OpenSourceTeamWork\AgentAssets`；正式 `project` 与 `agent-assets` 只读。
- 公开匿名请求，遵守 CR-009 预算；不保存正文、图片、权重、Cookie、token 或长版权文本。
- 角色/画风/特征/slider 与模型、训练粒度、网络算法分开建模；unknown 不得填默认值。
- 外部工具配置不是产品模板；模板必须走真实页面 validator 和 negative control。

## Execution phases

### Phase 1 — Channel catalog and preflight

- Outputs: 更新 `02_来源与证据/external-channel-catalog.json`，登记类别、URL、证据层级、提取目标和请求预算；写入开工清单证据。
- Completion: 新增渠道去重、无私有凭据、请求切片可重放。

### Phase 2 — Bounded harvest rounds

- Outputs: `02_来源与证据/external-harvest/request-log-*.jsonl`、summary、failure report、source registry 增量。
- Completion: 每轮 ≤20 请求；成功、404、超时、截断均保留；只记录状态/大小/hash/revision。

### Phase 3 — Knowledge synthesis

- Outputs: `04_知识库候选/external-channels/*.md` 及其它主题文档、eval 草稿、覆盖矩阵。
- Completion: 每篇含 Version/Scope/Evidence status、来源、边界、别名和 eval；lint 与引用检查全绿；自由文本抽取保留规则/置信度/缺失率。

### Phase 4 — Template probing

- Outputs: 新增 TOML + evidence card，或 `research-rejected/` 失败样本及断言。
- Completion: 根目录模板 TOML parse、显式 `model_train_type`、真实宿主 validator `ok`、negative control 非 `ok`、normalized diff 可解释；skip/redirect/reject 不算通过。

### Phase 5 — Evaluation and gate

- Outputs: Stage 7 gate、cleanup report、刷新 migration manifest/preview、Zero-Short/hash/diff 证据。
- Completion: P0/P1=0；候选知识与模板全部可追踪；正式仓库 Git 状态与开工基线一致；最终状态 `awaiting-user-approval`。

## Decision log

- Verified: Stage 0..6 门禁已通过；Stage 7 执行前候选为 54 篇知识、12 份根模板；完成后为 64 篇知识、13 份根模板。
- Locked: 新渠道优先知识增量；模板只接受当前 validator 可证明的组合。
- Assumption: 公共 API/模型卡字段可用于来源与缺失率观察，不用于训练效果或推荐参数因果结论。
- Open: 外部页面可能代理失败或发生 revision 漂移；失败必须留档并允许后续重放。

## Critical artifacts and validation

- Harvester: `tools\external_channel_harvest.py`
- Validation: `tools\stage2_lint.py`、`tools\stage3_validate_templates.py`、`tools\stage4_eval_review.py`、`tools\stage4_migration_manifest.py`、`tools\stage4_zero_short.py`
- Evidence root: `06_评测与校验\evidence\stage-7`

## Progress ledger

- Phase 1: `done`
- Phase 2: `done` — 39/39 bounded requests HTTP 200; truncation states retained.
- Phase 3: `done` — 64 candidate knowledge docs, 64 eval mappings, lint green.
- Phase 4: `done` — 13 root templates validator `ok`; negative controls non-ok; 2 rejected samples retained.
- Phase 5: `done` — evaluation review, 91-op migration preview and Zero-Short all pass.
- Overall progress: `done (pass-with-boundary)`
- Next action: user review of `07_迁移包/migration-preview.md` and `migration-manifest.json`; migration requires separate explicit approval.
